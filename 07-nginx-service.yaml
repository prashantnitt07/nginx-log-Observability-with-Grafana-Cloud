apiVersion: v1
kind: Service

metadata:
  name: nginx-poc
  namespace: poc

spec:

  selector:
    app: nginx-poc

  ports:

    - name: http
      port: 80
      targetPort: 80
      nodePort: 30900
